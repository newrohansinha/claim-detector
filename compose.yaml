services:
  api:
    build:
      context: .
    image: claim-detector:local
    ports:
      - "8000:8000"
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    environment:
      CLAIM_DEVICE: cpu
